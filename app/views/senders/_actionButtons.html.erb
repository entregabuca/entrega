  <div class="d-inline mt-2 mb-2">  
    <%= link_to "#{Sender.human_attribute_name(:back_to_sender)}", sender_path, :class => "btn btn-primary btn-back" %> 
  </div>

  <% if current_page?(controller: 'senders', action: 'edit')  %> 
    <div class="d-inline mt-2 mb-2">      
        <%= form.submit Sender.human_attribute_name(:update), :class => ' btn btn-primary btn-update' %><!-- 2B deleted btn-post -->     
    </div>
  <% end %>
